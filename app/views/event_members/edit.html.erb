<!-- <%= form_for @invite , :url => invites_path do |f| %>
    <%= f.hidden_field :event_member_id, :value => @invite.event_member_id %>
    <%= f.label :email %>
    <%= f.email_field :email %>
    <%= f.submit 'Send' %>
<% end %> -->
